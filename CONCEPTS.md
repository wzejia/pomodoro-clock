# Concepts

> Shared domain vocabulary for this project — entities, named processes, and status concepts with project-specific meaning. Seeded with core domain vocabulary, then accretes as ce-compound and ce-compound-refresh process learnings; direct edits are fine. Glossary only, not a spec or catch-all.

## 验收文化

### 明卷
任务书里逐条列出的显性验收标准，完成判定以「每条都有可核对的证据」为准，不接受自我感觉。证据指到具体文件名、命令输出或实测数值。

### 三件套
每次非平凡任务收口时必须同步维护的三份断点文档：进度断点、开发日志、待裁决清单。三者分别回答「现在到哪了」「做了什么怎么验的」「什么还没裁决」。

### 评审循环
视觉改动后的标准验收流程：采集截图 → 派独立评审子智能体（只给截图与 Apple HIG，不给实现上下文）→ 按 P1/P2/P3 分诊 → 修 P1 重采重审。

评审循环的收敛判据是连续两轮无 P1（有封顶轮数）；采集事故（截图没采到或采错态）不等于产品缺陷，但当轮严格口径重计连续轮数。P2 立修或留裁，P3 记录可不修但必须写理由。

### 证据链
验收结论所依赖的完整证据序列：动作前的状态确证、动作过程的存证、动作后由独立通道裁决的结果验证。链条上任何一环靠「应该成了」推断，结论都不算数。

### 像素校准
视觉改动不以肉眼描述为准、以采样数值为准的校准纪律：改动前后对固定采样点取 RGB（或对区域取亮度分布），用 Δ 数值判定「变化是否存在、量级是否足够、有没有过火」。评审员同样按此口径自证结论——数值比形容词硬。

## 采集与注入

### 采集管线
把应用摆到指定状态并截图存证的标准脚本链。起点总是迷你收起态，结束必须恢复起始态，否则污染下一轮采集首帧。

### 八态
评审的标准截图集合：迷你、展开面板、设置抽屉、右键菜单四态 × 明暗两主题。功能版本可在其后追加特性专属状态。

### 相位归一
采集时敏帧（计时数字/进度条）前，把活体计时器设到确定性的已知相位再截图，禁止在任意相位上「碰巧截」；配套断言硬化——帧内数值与 snapshot 逐项比对，不匹配判 FAIL 而非静默出图。针对的坑是接管间隙计时器自然推进导致前后帧串相（v11 进度条采集翻车所立）。

### 物理注入
通过 OS 级接口（移动真实光标、注入按键）驱动应用的验证通道，用于焦点切换、全局快捷键、托盘、跨窗口拖拽这类页内通道够不着的动作。物理注入链的纪律是「先验证再按键」：落点先经独立通道确证、破坏性按键前先截图、效果由应用状态裁决。

移动真实光标本身不向窗口投递悬停消息——要验证悬停态，须在落点后叠加一次微小相对位移的鼠标移动事件，否则页面悬停态零命中（易被误判为「窗口收不到真实输入」）。同一原理的对称面：把光标挪走也**不清**悬停残留，清除与制造走同一条路——都得靠真实移动消息让渲染器重算（v13 托盘采集四图 hover 钉死即「归位只挪光标」的半套用）。

### 合成事件
通过 CDP 向页面内派发的输入事件通道，坐标相对视口、与真实光标无关，适合页内悬停/按压/几何实测。合成事件不携带物理按键态——拖拽等依赖物理键态的判定以系统键态查询为真相源，页内事件信号（如 mouseleave）不是权威。

合成输入在渲染器内留下的悬停/焦点残留会跨窗口隐藏-显示存活（页面无 API 可清），长命实例采集前须把合成光标归位到角落或重启实例，否则截图带「幽灵悬停」假态，评审会据此误报产品缺陷。

## 窗口与材质

### morph
窗口形态切换的架构：窗口一次性瞬变到目标尺寸（含向工作区内的钳制），用户看到的形变动画由页内卡片在合成器上完成。窗口层与动画层分离，瞬变不可见、动画不掉帧。

### 迷你胶囊 / 展开面板
应用的两个常驻窗口形态：迷你胶囊是平时贴着的小条，点击即开（走宽容点击判定）；展开面板是完整计时与统计界面。两态间由 morph 过渡。

### 宽容点击
点击与拖拽的区分规则：按下后位移很小且很快松手的一律算点击；位移超过阈值才进入拖拽。拖拽一旦开始，光标跑出窗口不终止拖拽。

### 失焦即收
窗口失去前台焦点时自动从展开面板收回迷你胶囊的行为，回收应在转瞬之间完成。因本机环境限制，该行为用「程序抢前台」模拟失焦验收，并书面登记了这一环境偏离。

### 材质两档与几何不变
界面有两种材质档（液态玻璃 / 经典），材质切换只允许改绘制属性（背景、阴影、颜色、滤镜），禁止改任何几何属性——同一控件在两档下的位置尺寸必须逐像素相等，切换不得引起任何位移。

### 托盘菜单窗
第二个常驻隐藏窗口：右键系统托盘图标时贴图标弹出的自绘菜单（取代系统原生菜单），观感与应用内右键菜单同族。菜单项内容每次打开现查真相（计时状态定开始/暂停文案、注册表定自启勾选），失焦或 Esc 即关。

焦点语义与「失焦即收」正交：主窗把焦点让给托盘菜单窗不算失焦（不整收）；托盘菜单窗真正失焦而主窗仍展开且未聚焦时，补一次整收。

## 全局输入

### 快捷键改键
设置抽屉里的全局快捷键管理：键帽显示当前生效键，点击即录（修饰键+主键），录完试注册，被系统占用则提示并保留旧键，可一键关闭（关闭后保留按键记忆，重开恢复）。

配置键（持久化在 config.json）与生效键（实际注册成功的键）可能不同——启动时配置键注册失败会自动降级到备用键，一切以生效键为准。
